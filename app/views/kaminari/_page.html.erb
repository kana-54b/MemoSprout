
<% if page.current? %> <!-- 現在のページがアクティブの場合 -->
  <span class="page current join-item btn btn-active"><%= page %></span> <!-- ページ番号 -->
<% else %> <!-- そうでない時は -->
  <%= link_to page, url, class: "page join-item btn", remote: remote, rel: page.rel %> <!-- ページ番号をリンクに -->
<% end %>
